#!/bin/bash
# Renamed → "Start New Sibyl Project (Opus 5).command" (default is now Opus 5, 1M context).
# Kept so any old alias/Dock item still works. Safe to delete once you've switched over.
exec "$HOME/Desktop/Sibyl Projects/System/Start New Sibyl Project (Opus 5).command"
