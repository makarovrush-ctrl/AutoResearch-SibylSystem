{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/on-pre-read-doc.sh\""
          }
        ],
        "description": "Redirect PDF/Office/ebook reads to a markitdown Markdown sidecar to cut token cost"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/on-bash-complete.sh\""
          }
        ],
        "description": "Route sibyl CLI results: lark sync trigger + experiment monitor daemon launch"
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/on-session-start.sh\""
          }
        ],
        "description": "Initialize self-heal monitor and recover pending background processes"
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/on-session-start-shortcut.sh\""
          }
        ],
        "description": "Auto-create the conversation resume shortcut (CLAUDE.md Step Zero) so it can never be forgotten"
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/on-stop.sh\""
          }
        ],
        "description": "Clean up daemon PIDs for stopped/done projects"
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/on-stop-shortcut.sh\""
          }
        ],
        "description": "Retitle the auto-created shortcut from the transcript and write the Readable/*.md summary"
      }
    ]
  }
}
