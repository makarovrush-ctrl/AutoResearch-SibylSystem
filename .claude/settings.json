{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/plugin/hooks/scripts/on-pre-read-doc.sh\"",
            "timeout": 120,
            "statusMessage": "Converting document via markitdown to save tokens..."
          }
        ],
        "description": "Redirect PDF/Office/ebook reads to a markitdown Markdown sidecar to cut token cost. Mirrors plugin/hooks/hooks.json, which is inert because the sibyl-research plugin is not installed."
      }
    ]
  }
}
